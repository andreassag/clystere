nextflow_pipeline {
    name "Test pipeline default run"
    script "main.nf"
    profile "test,docker"
    options "-stub"

    test("Pipeline runs with test profile") {
        when {
            params {
                outdir = "$outputDir"
            }
        }
        then {
            assert workflow.success
        }
    }
}
